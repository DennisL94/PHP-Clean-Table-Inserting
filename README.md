# PHP-Clean-Table-Inserting
A simple way to insert data into an SQL table. Not fully completed for backend uses, but the main content is functional.
Also supports Password hashing, with cost value of 13.
